# Data + artifacts (large / regenerated)
data/raw/
data/masks/
data/features/
data/chunks/
data/*.tar.gz
data/*.zip
weights/
results/
backup_from_server/
plus/
phase4_review/
.venv/
venv/

# Archives / heavy generated binaries
*.zip
*.pdf

# Huge local manifests (regenerate on studio)
data/manifests/vessel_prob_v1.json
data/splits/loo_tmp/

# Local logs / temp
logs_*.txt
*.log
scripts/_tailscale_test.txt

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
.pytest_cache/

# Secrets
.env
.env.*
*.pem
credentials.json

# LaTeX build junk
*.aux
*.bcf
*.lof
*.lol
*.lot
*.out
*.toc
*.run.xml
*.synctex.gz
*.synctex(busy)
*.fls
*.fdb_latexmk
*.bbl
*.blg

# OS
.DS_Store
Thumbs.db

# Expert-validation build outputs and patient data (never commit)
expert_validation/package_build/
expert_validation/images/
expert_validation/*.zip
expert_validation/*.zip.sha256
expert_validation/grader_*/
expert_validation/adjudicated/
expert_validation/*.pdf


# --- Sensitive research metadata. Never commit. -------------------------------------------
# The private study key maps blinded IDs to source, label, split and local patient paths.
expert_validation/blinding_key.csv
*blinding_key*.csv
# Dataset manifest tables carry patient/exam identifiers, labels and absolute local paths.
# They are held privately; regenerate locally with src/data/prepare_split.
data/splits/all.csv
data/splits/train.csv
data/splits/val.csv
data/splits/test.csv
data/splits/group_assignments.csv
data/splits/excluded_ambiguous_exact_duplicates.csv
data/splits/missing_masks.csv
data/splits/rerun_masks.csv
# Clinical images: fundus photographs, masks, overlays and Grad-CAM renders.
docs/figures/vessel_overlay*.png
docs/figures/gradcam_*.png
*.zip

# --- Private audit and recovery scratch. Never commit. -------------------------------------
# Patient-linked inventories, recovered checkpoint candidates and reproduction scratch stores.
# Scientific identity of these tables is published as hashes only (see artifacts/).
_private/
_private_audit/
**/repro_scratch/

# The thesis source and its figure PDFs are deliverables, not regenerated data.
!thesis_v9/**/*.pdf
!report_assets_final/figures/**/*.pdf

